apiVersion: v1
kind: Service
metadata:
  name: {{ include "discord-house-cup.fullname" . }}
  labels:
    {{- include "discord-house-cup.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "discord-house-cup.selectorLabels" . | nindent 4 }}
